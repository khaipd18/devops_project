deployment: 
  enabled: false

replicaCount: 0

serviceAccount:
  create: false

service:
  enabled: true
  type: LoadBalancer
  port: 8080
  targetPort: 8080
  selectorOverride: "frontend"
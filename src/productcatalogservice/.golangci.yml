run:
  timeout: 5m
  issues-exit-code: 1

linters:
  disable-all: true
  
  enable:
    - gosimple     
    - ineffassign  
    - unused       

issues:
  exclude-rules:
    - linters:
        - staticcheck
      text: "SA1019:"